<h1>WELCOME HOME</h1>

<br>

<% if @user.id %>
    <img src="<%= @user.picture %>" alt="User's Picture" height="256" %>
    <h2 style="color:green"><%= @user.name %>,</h2>
    <h2 style="color:green">you are logged in :)</h2>
<% else %>
    <!--   <%= link_to(controller: 'sessions', action: 'new') { 'Log In' } %>   -->
    <%= button_to "Log In", login_path, method: "get" %>
    
    <br><br><br><br>
    
    <!--    <%= link_to 'Sign Up', signup_path %>    -->
    <%= button_to "Join us.", signup_path, method: "get" %>
<% end %>